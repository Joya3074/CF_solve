#include <iostream>
#include<string>
using namespace std;
int main()
{
    int x;
    cin >> x;
    while (x--)
    {
        string str = "codeforces";
        char ch;
        cin >> ch;
        bool a = false;
        for (int i = 0; i < str.length(); i++)
        {
            if (str[i] == ch)
            {
                a=true;
                break;
            }
        }
        if (a==true)
        {
            cout << "YES" << endl;
        }
        else
        {
            cout << "NO" << endl;
        }
    }
    return 0;
}
